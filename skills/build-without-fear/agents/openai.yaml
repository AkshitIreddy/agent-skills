interface:
  display_name: "Build Without Fear"
  short_description: "Build coherent state and recovery boundaries"
  default_prompt: "Use $build-without-fear to implement the required subsystem with explicit state, ownership, and recovery."
